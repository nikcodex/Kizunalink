# ╔════════════════════════════════════════════════════════════════╗
# ║  KizunaLink — Example Configuration                          ║
# ║                                                              ║
# ║  Copy this file to config.toml and customize for production. ║
# ║  Tokens shown as "your-*-token" must be replaced.            ║
# ║  Leave values empty or commented out to disable features.     ║
# ╚════════════════════════════════════════════════════════════════╝

[server]
address = "0.0.0.0"
port = 2333
authorization = "youshallnotpass"
player_update_interval = 5        # seconds
stats_interval = 30               # seconds
websocket_ping_interval = 20      # seconds
max_event_queue_size = 100

[player]
stuck_threshold_ms = 10000
buffer_duration_ms = 400
frame_buffer_duration_ms = 5000
resampling_quality = "medium"                                                  # low | medium | high
opus_encoding_quality = 10                                                     # 1-10
tape = { tape_stop = true, tape_stop_duration_ms = 500, curve = "sinusoidal" } # curve: linear | exponential | sinusoidal

[player.mirrors]
# List of mirror provider patterns. %ISRC% or %QUERY%
providers = ["tdsearch:%ISRC%","dzsearch:%ISRC%","jssearch:%QUERY%","amksearch:%QUERY%","gnsearch%QUERY%","dzsearch:%QUERY%","ytsearch:%ISRC%", "ytsearch:%QUERY%", "scsearch:%QUERY%"]

[player.mirrors.best_match]
# Enable weighted scoring to find the best-matching candidate.
# When false, the first resolvable result from each provider is accepted immediately.
# Default: true
scoring = true

# Prefixes treated as throttled (rate-sensitive) — tried sequentially after all parallel free providers.
# Default: ["ytmsearch:", "ytsearch:"]
throttled_prefixes = ["ytmsearch:", "ytsearch:"]

# Confidence thresholds (only used when scoring = true). All values in [0.0, 1.0].
# immediate_use   — accept instantly, cancel remaining parallel searches.
# high_confidence — try up to 2 candidates instead of 3.
# min_similarity  — discard anything below this score entirely.
immediate_use   = 0.88
high_confidence = 0.75
min_similarity  = 0.50

# Scoring weights (should sum to 1.0).
weight_title    = 0.50
weight_artist   = 0.30
weight_duration = 0.20

# Milliseconds within which two track durations are considered identical.
duration_tolerance_ms = 3000

[logging]
level = "info"              # trace | debug | info | warn | error | off
filters = "kizunalink=debug" # e.g. "kizunalink=debug,davey=off"

[logging.file]
path = "./logs/kizunalink.log"
max_lines = 10000
rotate_daily = false  # when true, creates kizunalink-YYYY-MM-DD.log each day
max_files = 7         # max daily log files to keep (0 = unlimited, only applies when rotate_daily = true)

[route_planner]
enabled = false
cidrs = []        # ["1.2.3.4/24"]
excluded_ips = []

[sources.youtube]
enabled = true
#refresh_tokens = [] # for tv client playback
get_oauth_token = false

[sources.youtube.clients]
search = ["MUSIC_ANDROID", "MUSIC_WEB", "ANDROID", "WEB"]
playback = ["ANDROID_VR", "TV_CAST", "WEB_EMBEDDED", "TV", "WEB", "IOS"]
resolve = ["WEB", "IOS", "MUSIC_WEB", "ANDROID", "TVHTML5_SIMPLY"]

[sources.youtube.cipher]
url = "https://cipher.kikkia.dev/"
# token = "your-auth-token"

[sources.spotify]
enabled = true
search_limit = 10
recommendations_limit = 10
playlist_load_limit = 6
album_load_limit = 6
playlist_page_load_concurrency = 10
album_page_load_concurrency = 5
track_resolve_concurrency = 50
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.jiosaavn]
enabled = true
search_limit = 10
recommendations_limit = 10
playlist_load_limit = 50
album_load_limit = 50
artist_load_limit = 20
# decryption = { secretKey = "your-secret-key" }
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.applemusic]
enabled = true
country_code = "us"
playlist_load_limit = 0
album_load_limit = 0
playlist_page_load_concurrency = 5
album_page_load_concurrency = 5
# media_api_token = "your-media-api-token"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.gaana]
enabled = true
search_limit = 10
playlist_load_limit = 50
album_load_limit = 50
artist_load_limit = 20
stream_quality = "high"  # low | medium | high
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.deezer]
enabled = false
# arls = ["your-arl-token"]
# master_decryption_key = "your-key"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.tidal]
enabled = false
country_code = "US"
hifi_apis = ["http://localhost:8000"]          # at least one HiFi API URL required
hifi_qualities = ["LOSSLESS", "HIGH", "LOW"]   
playlist_load_limit = 50
album_load_limit = 50
artist_load_limit = 20
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.soundcloud]
enabled = true
search_limit = 10
playlist_load_limit = 100
# client_id = "your-client-id"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.qobuz]
enabled = false
search_limit = 10
playlist_load_limit = 100
album_load_limit = 50
artist_load_limit = 20
# user_token = "your-user-token"
# app_id = "your-app-id"
# app_secret = "your-app-secret"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.yandexmusic]
enabled = false
search_limit = 10
playlist_load_limit = 10
album_load_limit = 10
artist_load_limit = 10
# access_token = "your-oauth-token"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.vkmusic]
enabled = false
search_limit = 10
playlist_load_limit = 1   # pages × 50 tracks
artist_load_limit = 1     # pages × 10 tracks
recommendations_load_limit = 10
# user_token = "your-vk-access-token"    # get from browser devtools: POST /?act=web_token → response → access_token
# user_cookie = "your-full-vk-cookie"    # get from browser devtools: POST /?act=web_token → request → cookie header
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.netease]
enabled = false
search_limit = 10
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.anghami]
enabled = false
search_limit = 10
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.shazam]
enabled = true
search_limit = 10
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.mixcloud]
enabled = true
search_limit = 10
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.bandcamp]
enabled = true
search_limit = 10
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.audiomack]
enabled = true
search_limit = 20
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.audius]
enabled = true
search_limit = 10
playlist_load_limit = 100
album_load_limit = 100
# app_name = "your-app-name"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.pandora]
enabled = false
search_limit = 10
playlist_load_limit = 100
# csrf_token = "your-csrf-token"
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.http]
enabled = true

[sources.local]
enabled = true

[sources.google_tts]
enabled = false
language = "en-US"

[sources.flowery]
enabled = false
voice = "Salli"
translate = false
silence = 0
speed = 1.0
enforce_config = false

[sources.reddit]
enabled = false
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.lastfm]
enabled = false
search_limit = 10
# Create your API key here: https://www.last.fm/api/account/create
# api_key = "your-lastfm-api-key"

[sources.twitch]
enabled = true
# client_id = "your-client-id"  # leave blank for auto-discovery from twitch.tv
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[sources.amazonmusic]
enabled = true
search_limit = 10
# proxy = { url = "http://proxy:8080", username = "user", password = "pass" }

[lyrics]
youtubemusic = true
lrclib = true
genius = true
deezer = true
yandex = true
musixmatch = true
letrasmus = true
netease = true

[filters]
volume = true
equalizer = true
karaoke = true
timescale = true
tremolo = true
vibrato = true
distortion = true
rotation = true
channel_mix = true
low_pass = true
echo = true
high_pass = true
normalization = true
chorus = true
compressor = true
flanger = true
phaser = true
phonograph = true
reverb = true
spatial = true


[metrics.prometheus]
enabled = false
endpoint = "/metrics"
