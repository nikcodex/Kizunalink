# ==============================================================================
#  🌸 KizunaLink — Multi-Arch Cloud CI/CD Build Pipeline
# ==============================================================================

name: Build KizunaLink Engine

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  # ============================================================================
  #  JOB 1: Linux x86_64 (For Standard Linux VPS / Cloud Servers)
  # ============================================================================
  build-linux-x86_64:
    name: Build (Linux x86_64)
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Source Code
        uses: actions/checkout@v4

      - name: 🦀 Install Rust Toolchain (x86_64)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: ⚡ Cache Cargo Dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: x86_64-unknown-linux-gnu

      - name: 🚀 Build Release Binary
        run: cargo build --release --target x86_64-unknown-linux-gnu

      - name: 📦 Upload x86_64 Binary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: kizunalink-linux-x86_64
          path: target/x86_64-unknown-linux-gnu/release/kizunalink
          if-no-files-found: error
          retention-days: 90

  # ============================================================================
  #  JOB 2: Linux ARM64 / aarch64 (For Termux / Mobile / ARM VPS / Raspberry Pi)
  # ============================================================================
  build-linux-arm64:
    name: Build (Linux ARM64 / Termux)
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Source Code
        uses: actions/checkout@v4

      - name: 🦀 Install Rust Toolchain (ARM64)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: ⚡ Cache Cargo Dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: aarch64-unknown-linux-gnu

      - name: 🛠️ Setup Zig
        uses: korandoru/setup-zig@v1
        with:
          zig-version: 0.13.0

      - name: 🛠️ Install cargo-zigbuild
        run: pip install cargo-zigbuild

      - name: 🚀 Build Release Binary (ARM64)
        run: cargo zigbuild --release --target aarch64-unknown-linux-gnu

      - name: 📦 Upload ARM64 Binary Artifact
        uses: actions/upload-artifact@v4
        with:
          name: kizunalink-linux-arm64
          path: target/aarch64-unknown-linux-gnu/release/kizunalink
          if-no-files-found: error
          retention-days: 90
