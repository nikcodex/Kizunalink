# KizunaLink — Master Fix & Optimization Tracker

> Single source of truth for the "5.8 → 9+ / 10" plan.
> Update the checkboxes here as items land — no need to re-paste the long list.

**Status legend**

| Mark | Meaning |
|------|---------|
| `[x]` | Done (verified in source) |
| `[~]` | Partial / partially addressed |
| `[ ]` | Not done yet |
| `[!]` | Deferred — needs a compiler/crates.io or is otherwise risky to do without one |

**Last verified:** 2026-09-13 (compile status verified via GitHub Actions CI — `cargo check`
and `cargo test` pass on `main`; local `cargo`/rustfmt still unavailable in the sandbox).

**Quick summary**

- Phase 0 (compile): **C01–C10 done**, plus a **CI-driven compile-fix pass** (`cargo check
  --workspace --all-targets` on PR #11) that resolved ~30 real errors (doubled/incorrect
  `crate::` paths, `engine::Mixer` re-export, `SessionContext::get_player` accessor, `Arc<dyn
  SessionContext>` → `&dyn SessionContext` call sites, missing parens on counter methods,
  `Option<f32>`/`Receiver<PooledBuffer>` test type fixes, `SourceError: From<StatusCode>`).
  Last 13-error batch landed in `91b911d`; **awaiting a green Check** to confirm the crate is
  clean end-to-end (kizuna-server was never reachable by the checker while the lib was red).
- Phase 1 (bugs/security): **mostly done** — a few low-risk items remain (B01, B03, B04, B14, B15, B22, B23, B25)
- Phase 2 (quality): **mostly done** — Q02 (async-trait removal) is the big remaining item
- Phase 3 (polish): **structural items done** (Docker merge, OpenAPI, justfile/Makefile, cargo-deny, tooling configs); tests/bench/fuzz/rate-limiting still open

---

## 0. Critical — Compile

- [x] **C01** — `crate::media::media::` doubled paths (0 occurrences remain)
- [x] **C02** — `crate::config::discord::player::` paths (0 occurrences remain)
- [x] **C03** — `crate::discord::discord::player::` paths (0 occurrences remain)
- [x] **C04** — `crate::config::media::` paths (0 occurrences remain)
- [x] **C05** — `config/mod.rs` bare module paths → absolute `crate::` paths
- [x] **C06** — `kizuna-server` module decls + re-exports (`pub use kizunalink::{common, discord, ...}`)
- [x] **C07** — orphan root `src/` deleted
- [x] **C08** — root `build.rs` removed; `kizunalink/build.rs` (pkg-config probe) kept
- [x] **C09** — duplicate `kizuna-server/{build.rs, config.example.toml, Dockerfile, def.Dockerfile, docker-compose.yml}` removed
- [x] **C10** — `librust_out.rlib` removed; `*.rlib` + `/bin/` in `.gitignore`

## 1. Bugs & Logic

- [x] **B01** — Mixer `mix_buf` writes now structural: `split_at_mut` + `take(n)` (no arithmetic slice ranges)
- [x] **B02** — Buffer pool bucket key mismatch (release now uses `aligned_size(capacity)` + `shrink_to`)
- [x] **B03** — Makeup gain derived as `1/(0.25 + Σ positive gains)` capped at the legacy `4.0` (flat-EQ behavior preserved exactly; boosted-EQ no longer relies on the clamp)
- [x] **B04** — Zero-gain band `continue` documented as intentional state-advance (anti-zipper on runtime filter updates)
- [x] **B05** — `timescale_buffer` drain policy documented (drains oldest, stereo-aligned)
- [x] **B06** — RoutePlanner empty `ip_indices` guarded (`first().copied().unwrap_or(0)`); lock released before IP calc
- [x] **B07** — Detached seek/spawn now logs on failed WS send (this session)
- [x] **B08** — `stop_player` sends real `track_info` (cloned before state mutation) (this session)
- [x] **B09** — Literal `"xxx"` session-id error path removed (0 matches)
- [x] **B10** — YouTube OAuth no longer prints refresh/access tokens (only public URL/code)
- [x] **B11** — Logger writer `eprintln!` retained with reason documented (it *is* the sink; `tracing` would recurse)
- [x] **B12** — `AudioError` expanded (Decoder/Opus/Resample/UnsupportedFormat/EmptyStream/Cancelled)
- [x] **B13** — `ServerContext`/`SessionContext` wired through player/manager code
- [x] **B14** — `StandardEngine::push` matches on `send`: warns and returns `false` on disconnect; blocking send kept as intentional backpressure (bounded `buffer_duration_ms/20` channel)
- [x] **B15** — Resolved as intentional: trait is the documented test seam for `AudioProcessor::with_engine` (kept by design)
- [x] **B16** — `HttpSource`/`reader` implement `is_seekable`
- [x] **B17** — `rustalink` → `kizunalink` rename (0 matches)
- [x] **B18** — `HttpClientPool` proxy-failure loudly logged + documented (intentional non-fatal)
- [x] **B19** — `config::load()` loud warning on `config.example.toml` fallback
- [x] **B20** — Request bodies demoted to `debug!` (PATCH player/session)
- [x] **B21** — `validate_filters` generated by `define_filters!` macro
- [x] **B22** — Contract pinned by `timescale_frame_exchange_contract` test (no in-place replacement, ordered front-drain, atomic short-fill, reset clears exchange buffer)
- [x] **B23** — `soft_clip_i16` exp-knee limiter (transparent ≤0.9 FS, asymptotic saturation) in both `AudioMixer::mix` and `Mixer` output stage; tests for bounds/monotonicity/transparency
- [x] **B24** — DAVE `DAVE_MIN_VERSION` const now `panic!`s with a clear message (this session)
- [ ] **B25** — Heartbeat ack-timeout → zombie → reconnect integration test (not written)
- [x] **B26** — Opus encoder no longer `assert!`s (returns `Result`)
- [x] **B27** — Opus `encode` frame-size integer-division handled via `Result`
- [x] **B28** — `lib.rs` module decls match on-disk tree

## 2. Code Quality / Refactors

- [x] **Q01** — `define_filters!` macro collapses filter boilerplate
- [!] **Q02** — Remove `async-trait` (50 files still use `#[async_trait]`; traits are `dyn`-dispatched so native AFIT needs manual future boxing — deferred, needs compiler)
- [~] **Q03** — Server deps use `workspace = true`; still re-declared (~27) rather than re-exported
- [x] **Q04** — `[workspace.dependencies]` introduced
- [~] **Q05** — Unwrap audit: **131 `.unwrap()` / 89 `.expect()` remain**; network/decoder paths partially converted
- [x] **Q06** — Poison recovery consistent (0 remaining `.lock().unwrap()` outside tests)
- [x] **Q07** — `SourceError` + `GatewayError` typed enums (thiserror + `From` impls)
- [x] **Q08** — `kizuna-server/src/lib.rs` re-exports (`common`, `discord`, `audio`, `sources`, `gateway`, `player`)
- [~] **Q09** — `rustfmt.toml` configured; `cargo fmt --all` not runnable in this sandbox
- [x] **Q10** — RoutePlanner `#[allow(dead_code)]` removed (3 legit ones remain: yandexmusic, youtube, opus/decoder)
- [x] **Q11** — Codec shim kept + `opus_decoder.rs` counterpart added
- [ ] **Q12** — `#[track_caller]` on public panics (0 currently)
- [x] **Q13** — `const _: () = assert!(TARGET_SAMPLE_RATE == 48_000)`
- [ ] **Q14** — Shared source-plugin helpers (`get_json`, `match_url`, `search`) not extracted
- [~] **Q15** — Clippy not runnable here; `clippy.toml` present, CI enforces `-D warnings`
- [~] **Q16** — `config` still uses glob re-exports (`pub use filters::*;` etc.)
- [x] **Q17** — Crate-level docs for `engine`, `media`, `discord`, `lavalink`, `opus`
- [x] **Q18** — README expanded (239 lines): architecture, sources table, bot usage, API, vs-Lavalink, troubleshooting

## 3. Test Coverage

- [x] **T01** — Unit tests: **129 `#[test]`/`#[tokio::test]` attributes** (213 test fns)
- [ ] **T02** — Integration tests (`kizuna-server/tests/`) — needs `tower` dev-dep
- [ ] **T03** — Property tests (proptest/quickcheck) — needs dep
- [ ] **T04** — Benchmarks (criterion) — needs dep
- [ ] **T05** — Fuzz targets (cargo-fuzz) — needs dep

> T02–T05 need dev-dependencies. `crates.io` is blocked in this sandbox, but the new
> `fix.yml` `workflow_dispatch` inputs run `cargo add`/`cargo update` on the CI runner and
> commit the lockfile back — so T02–T05 are unblocked (batch 2).

## 4. Performance

- [ ] **P01** — Hot-loop vectorization (chunks_exact, `#[inline]`)
- [x] **P02** — `engine::disable_denormals()` sets MXCSR FTZ|DAZ in `main` *before* runtime construction so tokio workers + audio threads inherit it (x86; ARM FPCR documented as skipped)
- [ ] **P03** — `bytemuck`/SIMD buffer interpretation
- [ ] **P04** — Sharded/lock-free buffer pool
- [ ] **P05** — `HashMap<String, MixLayer>` → integer `LayerId`
- [x] **P06** — Config validation on startup (`AppConfig::validate()`)
- [x] **P07** — reqwest `http2` feature enabled
- [ ] **P08** — `youtube_client!` macro (13 clients, ~4.4k lines)
- [ ] **P09** — Source-resolution prefix lookup table (still linear `can_handle` scan)
- [~] **P10** — `lavalink_http_request_duration_seconds` (method/status, explicit buckets) + `lavalink_track_load_duration_seconds` shipped; encode-time histogram deferred (needs a lib-level registry)
- [x] **P11** — `overflow-checks = true` restored in dev profile
- [x] **P12** — `opt-level` profile tuning kept

## 5. Security

- [x] **S01** — OAuth token leak (= B10)
- [x] **S02** — Request-body logging (= B20)
- [x] **S03** — Constant-time auth compare (`subtle::ConstantTimeEq`)
- [x] **S04** — WS validates `Authorization`, `User-Id`, `Client-Name` headers
- [!] **S05** — `FailingAddress` machinery + REST free/expire paths verified; auto-`mark_failed` needs per-request IP↔result plumbing across 13 source clients — deliberately deferred (design task), manual endpoints work
- [x] **S06** — Per-IP token-bucket limiter (`kizuna-server::api::rate_limit`), 429 + Retry-After, idle-bucket GC, config `server.rate_limit_per_minute` (default 2000, 0=off), unit-tested pure logic
- [x] **S07** — No `CorsLayer` → browsers default to same-origin (verified; not permissive)
- [x] **S08** — Proxy fallback documented (= B18)
- [x] **S09** — `deny.toml` + CI `cargo deny check advisories` (this session)
- [x] **S10** — Docker hardened (non-root, `cap_drop: ALL`, `no-new-privileges`, read-only)

## 6. CI / DevOps

- [x] **D01** — CI: check / fmt / clippy / test / build (ubuntu + macos + **windows**) / **deny**; every job now emits failure annotations (fmt diff, clippy lints, build errors, deny findings) — no more blind iteration
- [x] **D09** — `fix.yml` auto-fix workflow: pushes `cargo fmt` + `cargo fix` + `clippy --fix` back to `arena/**` branches; dispatch inputs run `cargo add`/`cargo update` with runner network (the only place crates.io is reachable)
- [x] **D10** — Dead `deploy-docs.yml` removed (Astro site no longer in repo)
- [x] **D02** — Release: linux amd64+arm64, macOS amd64+arm64, Windows amd64+arm64, Termux arm64, multi-arch GHCR, assets
- [x] **D03** — Single multi-stage `Dockerfile` (builder + ci + local); `def.Dockerfile` removed (this session)
- [x] **D04** — `rust-toolchain.toml` pins stable + rustfmt + clippy
- [x] **D05** — `clippy.toml`, `deny.toml`, `.taplo.toml` (this session)
- [x] **D06** — `docs/openapi.yaml` REST API spec (this session)
- [x] **D07** — `justfile` + `Makefile` shortcuts (this session)
- [x] **D08** — `SECURITY.md` + `CONTRIBUTING.md` + issue/PR templates

## 7. Nits

- [x] **N01** — `AnyResult` used consistently (no `anyhow`)
- [x] **N02** — `player_update_interval` is `Duration` (TOML wire format unchanged: integer seconds); `PlaybackStartConfig.update_interval` + startup validation for zero
- [ ] **N03** — `EndTime::Set/Clear` → `Option<u64>` untagged (optional)
- [ ] **N04** — `tracing::span!` with structured fields
- [x] **N05** — `deny(clippy::print_stdout/print_stderr/dbg_macro)` at both crate roots (non-test), with locally allow-listed exceptions (banner, interactive OAuth, logger sink)
- [x] **N06** — `rustfmt.toml`: doc-comment formatting, wrap comments, `comment_width = 100`
- [x] **N07** — `utils::colors_enabled()` (NO_COLOR/CLICOLOR/CLICOLOR_FORCE/TERM=dumb/TTY) gates banner ANSI and tracing `with_ansi`
- [x] **N08** — README supported-sources checklist table
- [x] **N09** — `config.example.toml` header: "leave empty to disable"
- [~] **N10** — `[profile.dev.package."*"] opt-level = 3` kept (intentional for iteration speed)

---

## Session log

### 2026-09-12 (current session)

- **Verified** Phase 0–2 state statically (no compiler available): see checkboxes above.
- **Fixed**: B07 (warn on failed WS send + serialization failure in `session.rs`).
- **Fixed**: B08 (`stop_player` clones real `track_info` before clearing player state).
- **Fixed**: B24 (clear const-panic message for `DAVE_MIN_VERSION`).
- **Added**: merged `Dockerfile` (D03), `deny.toml` + `.taplo.toml` (D05/S09),
  `docs/openapi.yaml` (D06), `justfile` + `Makefile` (D07).
- **CI**: added `cargo deny check advisories` job + Windows to the build matrix (D01/S09).
- **Compile-fix pass (PR #11, via CI `cargo check` error annotations)**:
  - `486d6e8` — CI: `--color=never` so error annotations parse cleanly.
  - `893bbf2` — re-export `engine::{AudioMixer, Mixer}`; add `SessionContext::get_player()`
    (+ `Session` impl); drop nested `crate::` inside `use crate::{…}` (http/local/qobuz/
    youtube-cipher); `super::protocol::{GatewayPayload, OpCode}` in `handler.rs`;
    `protocol::builders::{resume, identify}` in `session/mod.rs`; `crate::lavalink::protocol::
    tracks::{Track::decode, TrackInfo}` in `start.rs`; `crate::config::lyrics::YandexLyricsConfig`.
  - `91b911d` — `Arc<dyn SessionContext>::as_ref()` at `&dyn SessionContext` call sites;
    `total_sent/total_nulled_historical()` parens before `fetch_add`; filters tests `Some(50.0)`/
    `Some(100.0)`; mixer tests pass `Receiver<PooledBuffer>`; `SourceError::HttpStatus(#[from]
    reqwest::StatusCode)`.
  - `efa666d` — CI showed `cargo check` down to **1 error** after `91b911d`: thiserror's
    `#[from]` on `reqwest::StatusCode` requires `StatusCode: std::error::Error` (it isn't), so
    switched to `HttpStatus(reqwest::StatusCode)` + manual `impl From<reqwest::StatusCode>`.
    Also cleared 3 of the 8 `cargo check` warnings (unused `default_tidal_quality` in
    `tidal.rs`, unused `OpusError` in `engine/engine/encoder.rs`, unused `use super::ffi` in
    `opus/error.rs`) and removed redundant `&(dyn …)` parens / trailing blank lines in
    `manager/{error,lyrics,start}.rs`.
  - **Status**: merged to `main` via PR #11 + PR #12. **`cargo check --workspace
    --all-targets` PASSES and `cargo test --workspace --all-targets` PASSES** (confirmed by
    CI on `3b0d170`). Remaining CI reds: Clippy (`-D warnings`), Formatting
    (`cargo fmt --check`), `Build (windows-latest)` release build, and Cargo Deny
    (advisories, non-blocking). These are pre-existing/known and need a Rust toolchain
    (rustfmt/clippy iteration) and Windows log access to resolve.

### 2026-09-13 (this session — "Kizunalink Production Grade" campaign)

- Environment: sandbox has **no cargo and no crates.io network** (GitHub-only). Therefore:
  CI is the toolchain. Built `fix.yml` (auto-pushes mechanical fixes back to the branch) and
  rewrote `ci.yml` so *every* job surfaces failures as annotations (the old session was blind
  for clippy/fmt/windows).
- **Batch 1** (PR #14): B01 B03 B04 B14 B15 B22 B23 | N02 N05 N07 | P02 P10 (partial) | S06 |
  S05 deferred-with-rationale. Removed dead `deploy-docs.yml`.
- **Pending CI verification**: batch 1 compiles/fmt/clippy status is unknown until the PR goes
  green; TODO flips below batch 1 assume CI acceptance.

### 2026-09-13 (continuation)

- Sandbox re-cloned the repo between turns (local git history reset to `main`, working tree
  preserved as uncommitted changes); recovered by re-pointing the branch at the remote head
  and committing the preserved diff as one consolidated commit (`572e361`).
- Pushed everything and merged **PR #11** (all compile fixes) and **PR #12** (perf_monitor
  dep + remaining warnings + `main.rs` `kizuna_server::` paths) to `main`.
- `kizunalink` lib: added `SourceError::HttpStatus` + manual `From<StatusCode>`; removed
  unused imports; removed `mut` on `&self`-method encoder bindings; dropped dead
  `build_filters` stub; CI now also surfaces warnings as annotations.
- `kizuna-server`: declared `perf_monitor` as a direct dep; fixed the binary's `crate::` →
  `kizuna_server::` module paths (the bin crate root is `main.rs`, not `lib.rs`).

### Prior sessions (PR #10)

- Phase 0 path fixes, workspace deps, filter macro, typed errors, module docs, README,
  test suite expansion, config validation, CI/security hardening, Docker hardening.
