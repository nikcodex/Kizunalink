services:
  kizunalink:
    build: .
    volumes:
      - ./config.toml:/config.toml:ro
    ports:
      - "2333:2333"
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
